---
"@openfort/react-native": patch
---

Update dependencies and resolve `pnpm audit` advisories. Bumps `react-native-passkeys` and dev tooling (Biome, changesets), and adds `pnpm.overrides` for transitive advisories (`axios`, `form-data`, `ws`, `shell-quote`, `tmp`, `@xmldom/xmldom`, `uuid`, `brace-expansion`, `linkify-it`, `markdown-it`, `js-yaml`, `@babel/core`) so `pnpm audit` reports no known vulnerabilities.
